@* <div class="d-flex flex-column align-items-center mt-4">

    <div class="rounded-3 bg-white p-3 px-4 shadow-sm">

        <MudPagination Color="Color.Primary"
                       Variant="Variant.Outlined"
                       Count="@PaginationResult.TotalPage"
                       Selected="@PaginationResult.PageNumber"
                       SelectedChanged="OnPageChanged" />

    </div>

    <MudText Typo="Typo.body2" Class="text-muted mt-2">
        Page @PaginationResult.PageNumber of @PaginationResult.TotalPage
    </MudText>

</div>


@code {
    [Parameter]
    public BasePaginationResult PaginationResult  { get; set; }

    [Parameter]
    public EventCallback<BasePaginationRequest> OnPageChanged { get; set; }
} *@